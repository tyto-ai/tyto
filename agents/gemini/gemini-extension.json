{
  "name": "tyto",
  "version": "0.6.2-1",
  "description": "Persistent memory and code intelligence for AI agents",
  "mcpServers": {
    "tyto": {
      "command": "${extensionPath}/../shared/scripts/tyto.cmd",
      "args": ["serve"],
      "env": {
        "TYTO_PLUGIN_DATA": "${extensionPath}/data"
      },
      "description": "tyto MCP server for memory and code intelligence"
    }
  },
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "Remote Auth Token",
      "description": "Auth token for remote Turso database (if using remote storage mode)",
      "envVar": "TYTO__MEMORY__REMOTE_AUTH_TOKEN",
      "sensitive": true
    }
  ]
}
